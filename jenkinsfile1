pipeline {
    agent any

    stages {

        stage('Clone Repo') {
            steps {
                checkout scm
            }
        }

        stage('Create Pod in Kubernetes') {
            steps {
                sh 'kubectl apply -f pod.yaml'
            }
        }

    }
}
